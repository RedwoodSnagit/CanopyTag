"""Legacy package setup kept to demonstrate deprecated file status."""

from setuptools import setup, find_packages

setup(
    name="forest-repo-demo",
    version="0.1.0",
    packages=find_packages(),
)
